# v5.12.4 전체 리체크 결과

검토 기준: v5.12.3 소스 전체 메뉴/권한/업무/자료실/직원관리/메일서명/전자결재 및 포함 SQL 정적 검토.

## 발견 및 수정
1. **업무 상태 DB 누락 (중요)**: 화면에는 `보완중`, `보류`가 있으나 기존 `task_status` enum에는 없어서 저장 시 오류 가능. v5.12.4 SQL에서 두 값 추가.
2. **파일 휴지통 RLS 오류 (중요)**: 관리자 파일 이동 시 `project_file` UPDATE WITH CHECK가 실패할 수 있는 경로 확인. 별도 `app_is_file_manager()` 보안 함수로 정책 재구성.
3. **파일 권한 누락 가능성 (보안)**: 수동 수정 과정에서 `files` 메뉴 권한 체크가 빠질 수 있어 project_file/storage 정책에 `app_has_permission('files')`를 다시 고정.
4. **전자결재 메뉴 권한과 DB 권한 불일치 (보안)**: 메뉴 OFF여도 기존 RLS가 데이터 접근을 허용할 수 있어 approval RLS에 `app_has_permission('approvals')` 추가.
5. **업무 상태 변경 오류 무시 (사용성)**: 상태 변경 실패 시 화면이 조용히 원복될 수 있어 오류 메시지 표시 추가.

## 확인된 기능
- 업무 배정: 프로젝트 최대 3개, 담당자 복수 선택, 업무 수정/삭제, 상태/우선순위/마감/보완사항.
- 프로젝트명(업무 분류): 추가/이름변경/목록 관리.
- 파일: 프로젝트·업무 첨부, 50MB 제한, 버전, 휴지통/복구/영구삭제, 자료실, 사용량 경고.
- 직원: 추가/수정/중지/삭제(소프트삭제)/복구, 메뉴 권한, 직원별 메일서명.
- 메일서명: 일반메일/견적/계약 발송 API에서 로그인 직원 프로필 사용.
- 전자결재: 상신/수정/취소/승인/반려/재상신/삭제 화면 구현.

## 주의/남은 운영 확인
- 직원 초대메일 방식은 서버의 `SUPABASE_SERVICE_ROLE_KEY`가 없으면 사용할 수 없음. 초기 비밀번호를 넣는 직접 생성 경로는 대체 동작이 있음.
- 실제 운영 DB는 현재 연결된 Supabase 커넥터에서 앱이 사용하는 `qcklrpnqqodvwazlvsqq` 프로젝트가 보이지 않아 라이브 DB 스키마를 직접 조회하지 못함. 따라서 이 리체크는 배포 패키지와 사용자가 실행한 SQL 기준 정적 검토임.
- Netlify 실제 빌드/브라우저 실기기 테스트는 배포 후 확인 필요.
