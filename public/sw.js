self.addEventListener('push',event=>{let d={title:'신양파트너스 알림',body:'새 업무 알림이 있습니다.',url:'/alerts'};try{d={...d,...event.data.json()}}catch(e){}event.waitUntil(self.registration.showNotification(d.title,{body:d.body,icon:'/icon.png',badge:'/icon.png',data:{url:d.url}}))});self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(cs=>{for(const c of cs){if('focus'in c)return c.focus()}return clients.openWindow(event.notification.data?.url||'/alerts')}))})
