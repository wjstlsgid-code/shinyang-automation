# v5.7.9

- Netlify TypeScript build error fixed in `app/quotes/page.tsx`.
- Responsive quote preview logic preserved.
- Hiworks SMTP mail features preserved.
- Fix: ResizeObserver effect no longer references `selectedItems` before declaration.
